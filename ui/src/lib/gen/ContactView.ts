// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ConnectionKind } from "./ConnectionKind";
import type { ConnectionOrigin } from "./ConnectionOrigin";
import type { ContactStatus } from "./ContactStatus";
import type { IpVersion } from "./IpVersion";
import type { Transport } from "./Transport";

export type ContactView = { y7_id: string, nickname: string | null, status: ContactStatus, added_at: number, presence: ConnectionKind, 
/**
 * Transport of the live connection (QUIC / TCP), `None` when offline.
 */
transport: Transport | null, 
/**
 * IP family of the live connection (v4 / v6), `None` for relay / offline.
 */
ip_version: IpVersion | null, 
/**
 * HOW the live connection was established (direct dial / DCUtR / relay / …).
 */
origin: ConnectionOrigin, };
