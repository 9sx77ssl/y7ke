{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Y7KE capability — main window may invoke core commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
